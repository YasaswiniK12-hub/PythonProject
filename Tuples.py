# Tuple is a type of data structure. Has few key differences from list
coordinates=[(4,5),(6,7),(80,34)]
# Tupple is Immutable . round brackets . List square brackets
#coordinates[1]=10
# TypeError: 'tuple' object does not support item assignment
print(coordinates[1])
#List Mutable
# In real life scenarios they use tuples when they are sure that this doesn't need modification at any point of time
